01 WS-DATA PIC 9(5) VALUE ZERO.

* This statement explicitly initializes the numeric field WS-DATA to zero, which is the correct way to handle numeric data initialization. It makes the code more clear and reduces the chance of runtime errors.